{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "scrims",
  "title": "scrims",
  "description": "viewport fogs at top and bottom, scroll-linked, never transitioned",
  "registryDependencies": [
    "@ag/tokens"
  ],
  "files": [
    {
      "path": "registry/base-nova/ui/scrims.tsx",
      "content": "\"use client\"\n\n// Top and bottom viewport scrims, scroll-linked (see scrims.css for why they are never\n// transitioned). Pure CSS where animation-timeline: scroll() exists; a passive scroll\n// listener toggles data-on elsewhere. Place it inside the element that scrolls\n// (the document, or a scrolling inset): the timeline is scroll(nearest).\n\nimport * as React from \"react\"\nimport \"./scrims.css\"\n\nexport interface ScrimsProps {\n  /** A sticky blurred header IS a top scrim; such a page opts out of this one. */\n  top?: boolean\n  bottom?: boolean\n  className?: string\n}\n\nexport function Scrims({ top = true, bottom = true, className }: ScrimsProps) {\n  const topRef = React.useRef<HTMLDivElement>(null)\n  const bottomRef = React.useRef<HTMLDivElement>(null)\n\n  React.useEffect(() => {\n    if (CSS.supports(\"animation-timeline: scroll()\")) return\n    const scroller = (topRef.current ?? bottomRef.current)?.parentElement\n    if (!scroller) return\n    const target =\n      scroller.scrollHeight > scroller.clientHeight\n        ? scroller\n        : (document.scrollingElement as HTMLElement)\n    const measure = () => {\n      const y = target.scrollTop\n      const end = y + target.clientHeight >= target.scrollHeight - 140\n      topRef.current?.toggleAttribute(\"data-on\", y > 160)\n      bottomRef.current?.toggleAttribute(\"data-on\", !end)\n    }\n    const source = target === document.scrollingElement ? window : target\n    source.addEventListener(\"scroll\", measure, { passive: true })\n    window.addEventListener(\"resize\", measure)\n    measure()\n    return () => {\n      source.removeEventListener(\"scroll\", measure)\n      window.removeEventListener(\"resize\", measure)\n    }\n  }, [])\n\n  return (\n    <>\n      {bottom && (\n        <div\n          ref={bottomRef}\n          aria-hidden\n          className={`ag-scrim${className ? ` ${className}` : \"\"}`}\n          data-edge=\"bottom\"\n        />\n      )}\n      {top && (\n        <div\n          ref={topRef}\n          aria-hidden\n          className={`ag-scrim${className ? ` ${className}` : \"\"}`}\n          data-edge=\"top\"\n        />\n      )}\n    </>\n  )\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/base-nova/ui/scrims.css",
      "content": "/* scrims: viewport fogs that keep the section below from competing with the one on\n   screen. Masked backdrop-blur + gradient, no layout, no pointer. Their opacity is\n   scroll-LINKED, never transitioned: the top one fades in over the first 160px of\n   scroll, the bottom one fades out over the last 140px, and on a page that cannot\n   scroll the timeline is inactive so both rest at 0. A transitioned scrim (opacity\n   0→1 over 500ms) crossfades WHILE a smooth anchor scroll is flying; that was the\n   \"weird effects\" complaint. Linked to scroll it simply is where the scroll is. */\n\n.ag-scrim {\n  position: fixed;\n  right: 0;\n  left: 0;\n  z-index: 20;\n  /* A margin on a fixed box is part of where it lands: `top + margin + height +\n     margin + bottom` has to equal the viewport, so a parent's `space-y-16` (which in\n     Tailwind v4 is a margin-block-end on every child but the last) pushed the bottom\n     fog 64px UP off the edge, with the page showing underneath it. An overlay that\n     positions itself against the viewport takes no spacing from whatever it happens\n     to be nested in. */\n  margin: 0;\n  pointer-events: none;\n  opacity: 0;\n  backdrop-filter: blur(4px);\n  -webkit-backdrop-filter: blur(4px);\n}\n.ag-scrim[data-edge=\"top\"] {\n  top: 0;\n  height: 5rem;\n  background: linear-gradient(\n    to bottom,\n    color-mix(in oklab, var(--background) 90%, transparent),\n    color-mix(in oklab, var(--background) 35%, transparent) 50%,\n    transparent\n  );\n  mask-image: linear-gradient(to bottom, black 25%, transparent);\n}\n.ag-scrim[data-edge=\"bottom\"] {\n  bottom: 0;\n  height: 7rem;\n  background: linear-gradient(\n    to top,\n    color-mix(in oklab, var(--background) 90%, transparent),\n    color-mix(in oklab, var(--background) 35%, transparent) 50%,\n    transparent\n  );\n  mask-image: linear-gradient(to top, black 25%, transparent);\n}\n\n@keyframes ag-scrim-top {\n  from {\n    opacity: 0;\n  }\n  to {\n    opacity: 1;\n  }\n}\n@keyframes ag-scrim-bottom {\n  from {\n    opacity: 1;\n  }\n  to {\n    opacity: 0;\n  }\n}\n\n@supports (animation-timeline: scroll()) {\n  .ag-scrim[data-edge=\"top\"] {\n    animation: ag-scrim-top linear both;\n    animation-timeline: scroll(nearest);\n    animation-range: 0 160px;\n  }\n  .ag-scrim[data-edge=\"bottom\"] {\n    animation: ag-scrim-bottom linear both;\n    animation-timeline: scroll(nearest);\n    animation-range: calc(100% - 140px) 100%;\n  }\n}\n\n/* Without scroll-driven animations the component toggles these from a scroll\n   listener; still no transition, for the same reason. */\n@supports not (animation-timeline: scroll()) {\n  .ag-scrim[data-edge=\"top\"][data-on],\n  .ag-scrim[data-edge=\"bottom\"][data-on] {\n    opacity: 1;\n  }\n}\n",
      "type": "registry:ui"
    }
  ],
  "type": "registry:ui"
}