{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "telegram-chat",
  "title": "telegram-chat",
  "description": "a Telegram chat on an iPhone as a pure function of {messages, progress}: peer, bot and group shapes, streaming bodies, reactions, afterlife",
  "registryDependencies": [
    "@ag/tokens"
  ],
  "files": [
    {
      "path": "registry/base-nova/ui/telegram-chat.tsx",
      "content": "\"use client\"\n\n// A Telegram chat as a pure function of {script, progress}: a message array plays\n// out in order (bubbles land, a typed message fills the composer first, a rich body\n// streams block by block, reactions pop) so that progress 0..1 maps deterministically\n// onto the conversation: scrubbing forward, backward or jumping is idempotent and\n// side-effect-free. Three chat shapes, one component: peer (a private chat, no bot\n// chrome), bot (a DM with a bot: the Menu pill), group (left bubbles carry colored\n// sender labels and mini avatars). Telegram realism: typing lives in the HEADER,\n// reactions are pills on the bubble, avatars are real images or Telegram-gradient\n// initials, the webpage preview is the client's card. The mockup is a `figure`\n// whose chrome is decorative (aria-hidden), but links and previews are REAL anchors:\n// what looks clickable is clickable, and tabbable. Uncontrolled (no `progress`) it\n// plays ONCE when it enters the viewport, pauses off-screen, and renders the\n// completed state under prefers-reduced-motion. `from` shifts the start so the first\n// frame already shows a conversation, never an empty screen.\n\nimport * as React from \"react\"\nimport \"./telegram-chat.css\"\n\nexport type ChatKind = \"peer\" | \"bot\" | \"group\"\n\nexport interface ChatPreview {\n  /** Site name line, link-colored bold (\"YouTube\", \"GitHub\"). */\n  site: string\n  title: string\n  description?: string\n  /** Large image (16:9 cover) above the text. */\n  image?: string\n}\n\nexport interface ChatReaction {\n  emoji: string\n  /** Shown in groups only (a private chat has two people; its pill is the bare emoji). */\n  count?: number\n  /** \"timeline\": pops one beat after the message lands, scrubbable. \"afterlife\"\n   *  (default): wall-clock, starts seconds after the story completes, dribbles in,\n   *  counts keep creeping up forever; scrubbing cannot summon or rewind it. */\n  when?: \"timeline\" | \"afterlife\"\n}\n\nexport type ChatBlock =\n  | { kind: \"heading\"; text: string; emoji?: string }\n  | { kind: \"item\"; text: string; cite?: string }\n  | { kind: \"quote\"; text: string; by?: string; cite?: string }\n\nexport interface ChatMessage {\n  /** \"me\" is your bubble on the right. Any other string is a sender on the left; in a\n   *  group it is the colored label and picks the mini avatar. */\n  from: \"me\" | string\n  text?: string\n  /** The sender's photo (left bubbles in groups). */\n  avatar?: string\n  /** The quoted message this one replies to. */\n  reply?: { from: string; text: string }\n  preview?: ChatPreview\n  /** Inline-bot attribution above the body (\"via @xtldrbot\"). */\n  via?: string\n  /** A rich body that streams block by block (a bot's answer). */\n  blocks?: ChatBlock[]\n  /** Footer under the blocks: source link and time. Lands after the last block. */\n  meta?: { label: string; href: string; time: string }\n  /** The URL cites deep-link into (a YouTube timestamp becomes &t=). */\n  source?: string\n  /** Header status shown for a beat before this message lands (\"typing\"). */\n  typing?: string\n  /** Typed into the composer char by char before sending. \"me\" only. */\n  typed?: boolean\n  /** A token of `text` spotlit after sending, until the next message lands. */\n  emphasis?: string\n  /** \"instant\": blocks land whole instead of streaming. */\n  pace?: \"stream\" | \"instant\"\n  reactions?: ChatReaction[]\n}\n\nexport interface ChatScript {\n  kind: ChatKind\n  /** Header title: the person, the bot, or the group name. */\n  chatName: string\n  /** Sub-line under the name (\"online\" · \"bot\" · \"24 members\"). */\n  chatTag: string\n  avatar?: string\n  /** Telegram's animated profile video (mp4), looping muted over `avatar` as its\n   *  poster; poster only under prefers-reduced-motion. */\n  avatarVideo?: string\n  messages: ChatMessage[]\n  /** The easter egg for whoever stays: messages that arrive `at` seconds after the\n   *  story completes, on the wall clock. */\n  afterlife?: {\n    from?: string\n    avatar?: string\n    avatarVideo?: string\n    messages: { at: number; text: string }[]\n  }\n  /** Accessible description of the whole demo (the mockup is one picture). */\n  alt: string\n}\n\nexport interface TelegramChatProps {\n  script: ChatScript\n  /** 0..1 scrub position. Omit for the one-shot in-view autoplay. */\n  progress?: number\n  /** Floor for the animation. A number is a raw 0..1 position; {message: k} starts\n   *  at the beginning of message k's beat with everything before it on screen. */\n  from?: number | { message: number }\n  /** Autoplay ms (uncontrolled only). Defaults proportional to script length. */\n  duration?: number\n  /** Seconds added to the whole afterlife schedule (reactions + messages): lets a\n   *  phone that completes instantly wait for its neighbours' story. */\n  afterlifeDelay?: number\n  /** URL of Telegram's doodle pattern (telegram-tt's assets/pattern.svg), applied as\n   *  a CSS mask tinted per theme, exactly like the client. */\n  wallpaper?: string\n  theme?: \"dark\" | \"light\"\n  className?: string\n}\n\n// Telegram's sender palette: label colors and the matching avatar gradients. A name\n// hashes to a stable index so a sender keeps one identity everywhere.\nconst SENDER_COLORS = [\n  \"#e17076\",\n  \"#eda86c\",\n  \"#a695e7\",\n  \"#7bc862\",\n  \"#6ec9cb\",\n  \"#65aadd\",\n  \"#ee7aae\",\n]\nconst AVATAR_GRADIENTS: [string, string][] = [\n  [\"#ff885e\", \"#ff516a\"],\n  [\"#ffcd6a\", \"#ffa85c\"],\n  [\"#82b1ff\", \"#665fff\"],\n  [\"#a0de7e\", \"#54cb68\"],\n  [\"#53edd6\", \"#28c9b7\"],\n  [\"#72d5fd\", \"#2a9ef1\"],\n  [\"#e0a2f3\", \"#d669ed\"],\n]\n\nfunction senderIndex(name: string): number {\n  let sum = 0\n  for (const ch of name) sum += ch.codePointAt(0) ?? 0\n  return sum % SENDER_COLORS.length\n}\n\nfunction gradientOf(name: string): string {\n  const [a, b] = AVATAR_GRADIENTS[senderIndex(name)] as [string, string]\n  return `linear-gradient(180deg, ${a}, ${b})`\n}\n\n// ── the timeline: messages → weighted beats → visible state at progress ──\n//\n// Structural beats get fixed char-equivalent weights so the scrub spends real time on\n// them; text streams by its own length.\nconst BEAT = {\n  land: 50,\n  /** typed: chars weigh double (a person types slower than a stream lands), after a\n   *  digest pause to read what arrived, then a send beat, then a dwell on the sent\n   *  line while its emphasis pulses. */\n  typeChar: 2,\n  digest: 160,\n  send: 40,\n  dwell: 220,\n  typing: 90,\n  react: 60,\n  meta: 70,\n} as const\n\ninterface Beat {\n  /** The beat begins (typing status may show, composer may fill). */\n  start: number\n  /** Composer typing window (typed only). */\n  typeStart: number\n  typedEnd: number\n  /** The bubble is on screen from here. */\n  land: number\n  /** Blocks complete here, cumulatively. */\n  blockEnds: number[]\n  /** The meta row lands here (mid its beat). */\n  metaAt: number\n  /** Timeline reactions pop here. */\n  reactAt: number\n  end: number\n}\n\ninterface Timeline {\n  beats: Beat[]\n  total: number\n}\n\nfunction buildTimeline(script: ChatScript): Timeline {\n  let at = 0\n  const beats = script.messages.map((m) => {\n    const start = at\n    if (m.typing) at += BEAT.typing\n    let typeStart = at\n    let typedEnd = at\n    if (m.typed) {\n      if (m.from !== \"me\")\n        throw new Error('telegram-chat: only a message from \"me\" can be typed')\n      typeStart = at + BEAT.digest\n      typedEnd = typeStart + (m.text ?? \"\").length * BEAT.typeChar\n      at = typedEnd + BEAT.send\n    } else {\n      at += BEAT.land\n    }\n    const land = at\n    if (m.typed && m.emphasis) at += BEAT.dwell\n    const instant = m.pace === \"instant\"\n    const blockEnds = (m.blocks ?? []).map((b) => {\n      at += instant ? 0 : blockText(b).length\n      return at\n    })\n    let metaAt = at\n    if (m.meta) {\n      metaAt = at + BEAT.meta / 2\n      at += BEAT.meta\n    }\n    const reactAt = land + BEAT.react\n    return {\n      start,\n      typeStart,\n      typedEnd,\n      land,\n      blockEnds,\n      metaAt,\n      reactAt,\n      end: at,\n    }\n  })\n  return { beats, total: Math.max(1, at) }\n}\n\nfunction blockText(b: ChatBlock): string {\n  return b.kind === \"item\" ? `• ${b.text}` : b.text\n}\n\n// Telegram colors URLs and @mentions as links inside a bubble; a bare\n// \"youtube.com/...\" is a link to the client, no scheme needed.\nconst LINK_RE =\n  /((?:https?:\\/\\/)?[a-z0-9-]+(?:\\.[a-z0-9-]+)+(?:\\/[^\\s]*)?|@[a-z0-9_]{3,})/gi\n\nexport function hrefOf(token: string): string {\n  if (token.startsWith(\"@\")) return `https://t.me/${token.slice(1)}`\n  return /^https?:\\/\\//.test(token) ? token : `https://${token}`\n}\n\n/** The first URL inside a text, if any (the thing a chat is about). */\nexport function linkIn(text: string | undefined): string | undefined {\n  return (text ?? \"\").match(LINK_RE)?.find((t) => !t.startsWith(\"@\"))\n}\n\nfunction linkify(text: string): React.ReactNode {\n  const out: React.ReactNode[] = []\n  let offset = 0\n  for (const [i, part] of text.split(LINK_RE).entries()) {\n    out.push(\n      i % 2 === 1 ? (\n        <a\n          className=\"tgchat-link\"\n          key={offset}\n          href={hrefOf(part)}\n          target=\"_blank\"\n          rel=\"noopener noreferrer\"\n        >\n          {part}\n        </a>\n      ) : (\n        part\n      ),\n    )\n    offset += part.length\n  }\n  return out\n}\n\n// A cite is the bot's own citation into the source: on YouTube a timestamp deep-links\n// to that second (`&t=29s`); anywhere else it opens the page.\nfunction citeHref(source: string, cite: string): string {\n  const url = hrefOf(source)\n  const parts = cite.split(\":\").map(Number)\n  const isTime = parts.length >= 2 && parts.every((n) => Number.isInteger(n))\n  if (!isTime || !/youtu\\.?be/.test(url)) return url\n  const seconds = parts.reduce((acc, n) => acc * 60 + n, 0)\n  return `${url}${url.includes(\"?\") ? \"&\" : \"?\"}t=${seconds}s`\n}\n\n// The text with its spotlight token wrapped so it can pulse once sent. Splits on the\n// first whole-word match only.\nfunction emphasized(\n  text: string,\n  emphasis: string | undefined,\n  lit: boolean,\n): React.ReactNode {\n  if (!emphasis) return linkify(text)\n  const m = new RegExp(\n    `(^|\\\\s)(${emphasis.replace(/[.*+?^${}()|[\\]\\\\]/g, \"\\\\$&\")})(?=\\\\s|$)`,\n  ).exec(text)\n  if (!m || m.index === undefined) return linkify(text)\n  const start = m.index + (m[1] as string).length\n  const end = start + emphasis.length\n  return (\n    <>\n      {linkify(text.slice(0, start))}\n      <mark className=\"tgchat-emph\" data-on={lit || undefined}>\n        {emphasis}\n      </mark>\n      {linkify(text.slice(end))}\n    </>\n  )\n}\n\nfunction PreviewCard({ preview, url }: { preview: ChatPreview; url: string }) {\n  return (\n    <a\n      className=\"tgchat-preview\"\n      href={hrefOf(url)}\n      target=\"_blank\"\n      rel=\"noopener noreferrer\"\n    >\n      {preview.image && (\n        // biome-ignore lint/performance/noImgElement: any origin, sized by the card\n        <img src={preview.image} alt=\"\" />\n      )}\n      <div className=\"site\">{preview.site}</div>\n      <div className=\"title\">{preview.title}</div>\n      {preview.description && <div className=\"desc\">{preview.description}</div>}\n    </a>\n  )\n}\n\nfunction AvatarVideo({\n  className,\n  src,\n  poster,\n}: {\n  className: string\n  src: string\n  poster?: string\n}) {\n  const ref = React.useRef<HTMLVideoElement>(null)\n  React.useEffect(() => {\n    if (window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches)\n      ref.current?.pause()\n  }, [])\n  return (\n    <video\n      ref={ref}\n      className={className}\n      src={src}\n      poster={poster}\n      autoPlay\n      muted\n      loop\n      playsInline\n      tabIndex={-1}\n    />\n  )\n}\n\n// One avatar element for every slot: the profile VIDEO when there is one (loops\n// muted, the photo as poster), else the photo, else a gradient initial.\nfunction Avatar({\n  className,\n  name,\n  photo,\n  video,\n}: {\n  className: string\n  name: string\n  photo?: string\n  video?: string\n}) {\n  if (video)\n    return <AvatarVideo className={className} src={video} poster={photo} />\n  // biome-ignore lint/performance/noImgElement: any origin, a small round avatar\n  if (photo) return <img className={className} src={photo} alt=\"\" />\n  return (\n    <span className={className} style={{ background: gradientOf(name) }}>\n      {name.charAt(0)}\n    </span>\n  )\n}\n\n// ── the component ──\n\nexport function TelegramChat({\n  script,\n  progress,\n  from = 0,\n  duration,\n  afterlifeDelay = 0,\n  wallpaper,\n  theme = \"dark\",\n  className,\n}: TelegramChatProps) {\n  const timeline = React.useMemo(() => buildTimeline(script), [script])\n  const controlled = progress !== undefined\n  const [auto, setAuto] = React.useState(0)\n  const [aliveSec, setAliveSec] = React.useState(0)\n  const root = React.useRef<HTMLElement>(null)\n  const thread = React.useRef<HTMLDivElement>(null)\n  const floor =\n    typeof from === \"number\"\n      ? from\n      : (() => {\n          const beat = timeline.beats[from.message]\n          if (!beat)\n            throw new Error(\n              `telegram-chat: from.message ${from.message} outside 0..${timeline.beats.length - 1}`,\n            )\n          return beat.start / timeline.total\n        })()\n  // ~6ms per weighted char: a brisk stream, bounded both ways.\n  const autoDuration =\n    duration ??\n    Math.min(14000, Math.max(4000, (1 - floor) * timeline.total * 6))\n\n  // One-shot in-view autoplay (uncontrolled only): rAF advance, paused off-screen,\n  // reduced-motion renders the completed state, never replays (anti-strobe).\n  React.useEffect(() => {\n    if (controlled) return\n    const el = root.current\n    if (!el) return\n    if (window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches) {\n      setAuto(1)\n      return\n    }\n    let frame = 0\n    let last = 0\n    let value = 0\n    const tick = (now: number) => {\n      value = Math.min(1, value + (now - last) / autoDuration)\n      last = now\n      setAuto(value)\n      if (value < 1) frame = requestAnimationFrame(tick)\n    }\n    const io = new IntersectionObserver(\n      ([entry]) => {\n        cancelAnimationFrame(frame)\n        if (entry?.isIntersecting && value < 1) {\n          last = performance.now()\n          frame = requestAnimationFrame(tick)\n        }\n      },\n      { threshold: 0.35 },\n    )\n    io.observe(el)\n    return () => {\n      io.disconnect()\n      cancelAnimationFrame(frame)\n    }\n  }, [controlled, autoDuration])\n\n  const raw = controlled ? (progress as number) : auto\n  const eff = floor + (1 - floor) * Math.min(1, Math.max(0, raw))\n  const at = eff * timeline.total\n  const isGroup = script.kind === \"group\"\n  const completed = at >= timeline.total - BEAT.meta / 2 - 1e-6 || eff >= 1\n\n  // The afterlife clock: wall-clock seconds since the story completed. Immune to\n  // progress by construction: scrolling back pauses the clock but never rewinds it,\n  // so reactions and late messages only ever accumulate.\n  React.useEffect(() => {\n    if (!completed) return\n    const id = window.setInterval(() => setAliveSec((s) => s + 1), 1000)\n    return () => window.clearInterval(id)\n  }, [completed])\n\n  // The thread is a real scroller ONLY once the story has settled (data-settled\n  // unlocks overflow): while streaming the wheel belongs to the page, and the thread\n  // stays pinned to the bottom. Settled, you scroll up to what was summoned. A late\n  // message pulls it down only if you were already at the bottom, exactly the\n  // client's behaviour.\n  // biome-ignore lint/correctness/useExhaustiveDependencies: eff/aliveSec are the beats that grow the thread\n  React.useLayoutEffect(() => {\n    const el = thread.current\n    if (!el) return\n    const atBottom = el.scrollHeight - el.scrollTop - el.clientHeight < 40\n    if (!completed || atBottom) el.scrollTop = el.scrollHeight\n  }, [completed, eff, aliveSec])\n\n  // Afterlife reactions across every message, in script order: staggered arrivals\n  // with deterministic jitter, each pill lands at 1, climbs to its scripted count one\n  // press at a time, then (groups only) keeps creeping up forever.\n  let afterlifeIndex = 0\n  const arriveAt = (i: number) => afterlifeDelay + 3 + i * 6 + ((i * 7) % 7)\n  const reactionPills = (m: ChatMessage, beat: Beat) => {\n    const shown: { emoji: string; count: number }[] = []\n    for (const r of m.reactions ?? []) {\n      if (r.when === \"timeline\") {\n        if (at >= beat.reactAt)\n          shown.push({ emoji: r.emoji, count: r.count ?? 1 })\n        continue\n      }\n      const i = afterlifeIndex++\n      const since = aliveSec - arriveAt(i)\n      if (since < 0) continue\n      const target = r.count ?? 1\n      const climb = Math.min(target - 1, Math.floor(since / 5))\n      const eternal = isGroup\n        ? Math.max(0, Math.floor((since - 5 * (target - 1)) / 22))\n        : 0\n      shown.push({ emoji: r.emoji, count: 1 + Math.max(0, climb) + eternal })\n    }\n    if (shown.length === 0) return null\n    return (\n      <div className=\"tgchat-reacts\">\n        {shown.map((r) => (\n          <span className=\"tgchat-react\" key={r.emoji}>\n            {r.emoji}\n            {isGroup && <span className=\"n\">{r.count}</span>}\n          </span>\n        ))}\n      </div>\n    )\n  }\n\n  const senderLabel = (name: string) =>\n    isGroup ? (\n      <div\n        className=\"tgchat-from\"\n        style={{ color: SENDER_COLORS[senderIndex(name)] }}\n      >\n        {name}\n      </div>\n    ) : null\n\n  // Group chats put a mini avatar beside every left bubble, like Telegram does.\n  const leftRow = (\n    bubble: React.ReactNode,\n    name: string,\n    avatarUrl?: string,\n  ) =>\n    isGroup ? (\n      <div className=\"tgchat-rowline\">\n        <Avatar className=\"tgchat-mini\" name={name} photo={avatarUrl} />\n        {bubble}\n      </div>\n    ) : (\n      bubble\n    )\n\n  const renderBlock = (\n    b: ChatBlock,\n    key: number,\n    source: string,\n    chars?: number,\n  ) => {\n    const text = blockText(b)\n    const cite = b.kind === \"item\" || b.kind === \"quote\" ? b.cite : undefined\n    return (\n      <div\n        className={b.kind === \"heading\" ? \"h\" : b.kind === \"item\" ? \"i\" : \"q\"}\n        key={key}\n      >\n        {b.kind === \"heading\" && b.emoji && (\n          <span className=\"e\">{b.emoji}</span>\n        )}\n        {chars === undefined ? text : text.slice(0, chars)}\n        {chars === undefined && cite && (\n          <a\n            className=\"tgchat-cite\"\n            href={citeHref(source, cite)}\n            target=\"_blank\"\n            rel=\"noopener noreferrer\"\n          >\n            {cite}\n          </a>\n        )}\n        {chars === undefined && b.kind === \"quote\" && b.by && (\n          <span className=\"by\">{b.by}</span>\n        )}\n        {chars !== undefined && <span className=\"tgchat-caret\" />}\n      </div>\n    )\n  }\n\n  // Header status: a message's own `typing` for a beat before it lands, and a late\n  // message telegraphs itself 3 s ahead, the small magic Telegram does.\n  const typingMessage = script.messages.find((m, i) => {\n    const b = timeline.beats[i] as Beat\n    return m.typing && at >= b.start + BEAT.typing / 3 && at < b.land\n  })\n  const nextLate = script.afterlife?.messages.find(\n    (late) => aliveSec < afterlifeDelay + late.at,\n  )\n  const lateTyping = Boolean(\n    nextLate && aliveSec >= afterlifeDelay + nextLate.at - 3,\n  )\n  const typingLabel = typingMessage\n    ? (typingMessage.typing as string)\n    : lateTyping\n      ? \"typing\"\n      : null\n\n  // The composer: a typed message mid-flight.\n  const typingIndex = script.messages.findIndex((m, i) => {\n    const b = timeline.beats[i] as Beat\n    return m.typed && at > b.typeStart && at < b.land\n  })\n  const composing =\n    typingIndex >= 0 ? (script.messages[typingIndex] as ChatMessage) : null\n  const composingBeat =\n    typingIndex >= 0 ? (timeline.beats[typingIndex] as Beat) : null\n  const composerChars =\n    composing && composingBeat\n      ? Math.min(\n          Math.ceil((at - composingBeat.typeStart) / BEAT.typeChar),\n          (composing.text ?? \"\").length,\n        )\n      : 0\n\n  return (\n    <figure\n      ref={root}\n      className={`tgchat${className ? ` ${className}` : \"\"}`}\n      data-theme={theme}\n      data-settled={completed || undefined}\n      aria-label={script.alt}\n    >\n      <div className=\"tgchat-phone\">\n        <span className=\"tgchat-btn action\" />\n        <span className=\"tgchat-btn vol-up\" />\n        <span className=\"tgchat-btn vol-down\" />\n        <span className=\"tgchat-btn power\" />\n        <div className=\"tgchat-screen\">\n          {wallpaper && (\n            <div\n              className=\"tgchat-wall\"\n              style={{\n                WebkitMaskImage: `url(${wallpaper})`,\n                maskImage: `url(${wallpaper})`,\n              }}\n            />\n          )}\n          <div className=\"tgchat-island\" />\n          <div className=\"tgchat-status\">\n            <span>9:41</span>\n            <span className=\"radios\">\n              <svg\n                aria-hidden=\"true\"\n                viewBox=\"0 0 17 11\"\n                width=\"17\"\n                height=\"11\"\n              >\n                <g fill=\"currentColor\">\n                  <rect x=\"0\" y=\"7\" width=\"3\" height=\"4\" rx=\"1\" />\n                  <rect x=\"4.5\" y=\"5\" width=\"3\" height=\"6\" rx=\"1\" />\n                  <rect x=\"9\" y=\"2.5\" width=\"3\" height=\"8.5\" rx=\"1\" />\n                  <rect x=\"13.5\" y=\"0\" width=\"3\" height=\"11\" rx=\"1\" />\n                </g>\n              </svg>\n              <svg\n                aria-hidden=\"true\"\n                viewBox=\"0 0 26 11\"\n                width=\"26\"\n                height=\"11\"\n              >\n                <rect\n                  x=\"0.6\"\n                  y=\"0.6\"\n                  width=\"21\"\n                  height=\"9.8\"\n                  rx=\"2.8\"\n                  fill=\"none\"\n                  stroke=\"currentColor\"\n                  strokeWidth=\"1.2\"\n                  opacity=\"0.5\"\n                />\n                <rect\n                  x=\"23.4\"\n                  y=\"3.6\"\n                  width=\"2\"\n                  height=\"3.8\"\n                  rx=\"1\"\n                  fill=\"currentColor\"\n                  opacity=\"0.5\"\n                />\n                <rect\n                  x=\"2.2\"\n                  y=\"2.2\"\n                  width=\"14\"\n                  height=\"6.6\"\n                  rx=\"1.6\"\n                  fill=\"currentColor\"\n                />\n              </svg>\n            </span>\n          </div>\n          <div className=\"tgchat-header\">\n            <span className=\"tgchat-round\">\n              <svg\n                aria-hidden=\"true\"\n                viewBox=\"0 0 16 16\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                strokeWidth=\"1.6\"\n                strokeLinecap=\"round\"\n              >\n                <path d=\"M10 3 5 8l5 5\" />\n              </svg>\n            </span>\n            <div className=\"tgchat-card\">\n              <div className=\"tgchat-names\">\n                <strong>{script.chatName}</strong>\n                {typingLabel ? (\n                  <span className=\"typing\">\n                    <span className=\"tgchat-tdots\">\n                      <i />\n                      <i />\n                      <i />\n                    </span>\n                    {typingLabel}\n                  </span>\n                ) : (\n                  <span>{script.chatTag}</span>\n                )}\n              </div>\n            </div>\n            <Avatar\n              className=\"tgchat-avatar\"\n              name={script.chatName}\n              photo={script.avatar}\n              video={script.avatarVideo}\n            />\n          </div>\n          <div className=\"tgchat-messages\" ref={thread}>\n            <div className=\"tgchat-thread\">\n              {/* Messages are positional by design: their order IS their identity,\n                  and the array never reorders. */}\n              {script.messages.map((m, i) => {\n                const beat = timeline.beats[i] as Beat\n                if (at < beat.land) return null\n                const source = m.source ?? linkIn(m.text) ?? \"\"\n                const next = timeline.beats[i + 1]\n                const lit = Boolean(m.emphasis) && (!next || at < next.land)\n                const streaming = m.blocks !== undefined\n                let full = 0\n                while (\n                  full < beat.blockEnds.length &&\n                  (beat.blockEnds[full] as number) <= at\n                )\n                  full++\n                const prevEnd =\n                  full === 0\n                    ? beat.land + (m.typed && m.emphasis ? BEAT.dwell : 0)\n                    : (beat.blockEnds[full - 1] as number)\n                const partial =\n                  streaming &&\n                  full < (m.blocks as ChatBlock[]).length &&\n                  at > prevEnd\n                    ? (m.blocks as ChatBlock[])[full]\n                    : undefined\n                const body = (\n                  <>\n                    {m.reply && (\n                      <div className=\"tgchat-reply\">\n                        <strong>{m.reply.from}</strong>\n                        <span>{m.reply.text}</span>\n                      </div>\n                    )}\n                    {m.via && <div className=\"tgchat-via\">{m.via}</div>}\n                    {m.text && emphasized(m.text, m.emphasis, lit)}\n                    {m.preview && (\n                      <PreviewCard preview={m.preview} url={source} />\n                    )}\n                    {streaming &&\n                      (m.blocks as ChatBlock[])\n                        .slice(0, full)\n                        .map((b, k) => renderBlock(b, k, source))}\n                    {partial &&\n                      renderBlock(\n                        partial,\n                        full,\n                        source,\n                        Math.floor(at - prevEnd),\n                      )}\n                    {m.meta && at >= beat.metaAt && (\n                      <div className=\"tgchat-meta\">\n                        <a\n                          className=\"src\"\n                          href={m.meta.href}\n                          target=\"_blank\"\n                          rel=\"noopener noreferrer\"\n                        >\n                          {m.meta.label}\n                        </a>\n                        <span className=\"time\">{m.meta.time}</span>\n                      </div>\n                    )}\n                    {reactionPills(m, beat)}\n                  </>\n                )\n                if (m.from === \"me\")\n                  return (\n                    <div\n                      className={`tgchat-bubble user${m.preview || linkIn(m.text) ? \" link\" : \"\"}`}\n                      // biome-ignore lint/suspicious/noArrayIndexKey: positional by design\n                      key={i}\n                    >\n                      {body}\n                    </div>\n                  )\n                return (\n                  // biome-ignore lint/suspicious/noArrayIndexKey: positional by design\n                  <React.Fragment key={i}>\n                    {leftRow(\n                      <div\n                        className={`tgchat-bubble bot${streaming ? \" tgchat-summary\" : \"\"}${!streaming && (m.preview || linkIn(m.text)) ? \" link\" : \"\"}`}\n                      >\n                        {senderLabel(m.from)}\n                        {body}\n                      </div>,\n                      m.from,\n                      m.avatar,\n                    )}\n                  </React.Fragment>\n                )\n              })}\n              {script.afterlife?.messages\n                .filter((late) => aliveSec >= afterlifeDelay + late.at)\n                .map((late) => (\n                  <div className=\"tgchat-rowline\" key={late.at}>\n                    {script.afterlife?.avatar && (\n                      <Avatar\n                        className=\"tgchat-mini\"\n                        name={script.afterlife.from ?? script.chatName}\n                        photo={script.afterlife.avatar}\n                        video={script.afterlife.avatarVideo}\n                      />\n                    )}\n                    <div className=\"tgchat-bubble bot\">\n                      {linkify(late.text)}\n                    </div>\n                  </div>\n                ))}\n            </div>\n          </div>\n          {composing?.reply && composerChars > 0 && (\n            <div className=\"tgchat-replybar\">\n              <svg\n                aria-hidden=\"true\"\n                viewBox=\"0 0 24 24\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                strokeWidth=\"2\"\n                strokeLinecap=\"round\"\n                strokeLinejoin=\"round\"\n              >\n                <path d=\"M9 17l-5-5 5-5\" />\n                <path d=\"M20 18v-2a4 4 0 0 0-4-4H4\" />\n              </svg>\n              <div>\n                <strong>Reply to {composing.reply.from}</strong>\n                <span>{composing.reply.text}</span>\n              </div>\n            </div>\n          )}\n          <div className=\"tgchat-composer\">\n            {script.kind === \"bot\" && (\n              <span className=\"pill\">\n                <svg\n                  aria-hidden=\"true\"\n                  viewBox=\"0 0 24 24\"\n                  fill=\"none\"\n                  stroke=\"currentColor\"\n                  strokeWidth=\"2.2\"\n                  strokeLinecap=\"round\"\n                >\n                  <line x1=\"4\" y1=\"7\" x2=\"20\" y2=\"7\" />\n                  <line x1=\"4\" y1=\"12\" x2=\"20\" y2=\"12\" />\n                  <line x1=\"4\" y1=\"17\" x2=\"20\" y2=\"17\" />\n                </svg>\n                Menu\n              </span>\n            )}\n            <span className=\"cbtn\">\n              <svg\n                aria-hidden=\"true\"\n                viewBox=\"0 0 24 24\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                strokeWidth=\"1.8\"\n                strokeLinecap=\"round\"\n                strokeLinejoin=\"round\"\n              >\n                <path d=\"M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48\" />\n              </svg>\n            </span>\n            <div className=\"box\">\n              {composing && composerChars > 0 ? (\n                <span className=\"typed\">\n                  {(composing.text ?? \"\").slice(0, composerChars)}\n                  <span className=\"tgchat-caret\" />\n                </span>\n              ) : (\n                <span className=\"hint\">Message</span>\n              )}\n              <svg\n                aria-hidden=\"true\"\n                viewBox=\"0 0 24 24\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                strokeWidth=\"1.8\"\n                strokeLinecap=\"round\"\n                strokeLinejoin=\"round\"\n              >\n                <circle cx=\"12\" cy=\"12\" r=\"10\" />\n                <path d=\"M8 14s1.5 2 4 2 4-2 4-2\" />\n                <line x1=\"9\" y1=\"9\" x2=\"9.01\" y2=\"9\" />\n                <line x1=\"15\" y1=\"9\" x2=\"15.01\" y2=\"9\" />\n              </svg>\n            </div>\n            <span className=\"cbtn\">\n              <svg\n                aria-hidden=\"true\"\n                viewBox=\"0 0 24 24\"\n                fill=\"none\"\n                stroke=\"currentColor\"\n                strokeWidth=\"1.8\"\n                strokeLinecap=\"round\"\n                strokeLinejoin=\"round\"\n              >\n                <path d=\"M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z\" />\n                <path d=\"M19 10v2a7 7 0 0 1-14 0v-2\" />\n                <line x1=\"12\" y1=\"19\" x2=\"12\" y2=\"23\" />\n                <line x1=\"8\" y1=\"23\" x2=\"16\" y2=\"23\" />\n              </svg>\n            </span>\n          </div>\n          <div className=\"tgchat-gesture\">\n            <span />\n          </div>\n        </div>\n      </div>\n    </figure>\n  )\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/base-nova/ui/telegram-chat.css",
      "content": "/* telegram-chat. Every selector lives under .tgchat; no global selector may appear in\n   this file. Sizing is container-driven (cqw against the 356px design width), themes\n   are the two token sets on [data-theme]. */\n\n.tgchat {\n  margin: 0;\n  container-type: inline-size;\n  width: 100%;\n  max-width: 356px;\n  font-family: var(\n    --font-sans,\n    ui-sans-serif,\n    -apple-system,\n    BlinkMacSystemFont,\n    \"Segoe UI\",\n    sans-serif\n  );\n  font-synthesis: none;\n  text-rendering: optimizeLegibility;\n  --tg-ease: cubic-bezier(0.22, 1, 0.36, 1);\n}\n\n.tgchat[data-theme=\"dark\"] {\n  --tg-phone: linear-gradient(\n    160deg,\n    #6c7075 0%,\n    #4a4e54 22%,\n    #595e65 50%,\n    #43474d 78%,\n    #676c72 100%\n  );\n  --tg-phone-band: inset 0 0 0 1px rgba(255, 255, 255, 0.14);\n  --tg-phone-chamfer:\n    inset 0 1px 1px rgba(255, 255, 255, 0.4),\n    inset 0 -1px 1px rgba(255, 255, 255, 0.16);\n  --tg-phone-button: linear-gradient(180deg, #5c6167, #43474d);\n  --tg-lift: 0 24px 60px rgba(0, 0, 0, 0.45), 0 4px 14px rgba(0, 0, 0, 0.3);\n  --tg-screen: #101315;\n  --tg-island: #000000;\n  --tg-island-ring: rgba(255, 255, 255, 0.11);\n  --tg-status-ink: #e8e6e1;\n  --tg-bot-bubble: #1e2227;\n  --tg-user-bubble: #246fae;\n  --tg-on-accent: #ffffff;\n  --tg-border: #2b3036;\n  --tg-text: #e8e6e1;\n  --tg-muted: #a0a29e;\n  --tg-composer-icon: #c3c6c9;\n  --tg-quote-rule: #757c85;\n  --tg-accent: #246fae;\n  --tg-link: #6ab3f3;\n  --tg-doodle: rgba(255, 255, 255, 0.09);\n  --tg-gesture-pill: #6e767f;\n}\n\n.tgchat[data-theme=\"light\"] {\n  --tg-phone: #a2a6ad;\n  --tg-phone-band: inset 0 0 0 1px rgba(0, 0, 0, 0.1);\n  --tg-phone-chamfer:\n    inset 0 1px 1px rgba(255, 255, 255, 0.5),\n    inset 0 -1px 1px rgba(255, 255, 255, 0.24);\n  --tg-phone-button: linear-gradient(180deg, #b3b7bd, #8c9097);\n  --tg-lift:\n    0 24px 60px rgba(27, 29, 30, 0.22), 0 4px 14px rgba(27, 29, 30, 0.1);\n  --tg-screen: #ffffff;\n  --tg-island: #16181a;\n  --tg-island-ring: rgba(0, 0, 0, 0.22);\n  --tg-status-ink: #1c1c1a;\n  --tg-bot-bubble: #f1f1f1;\n  --tg-user-bubble: #0b74bd;\n  --tg-on-accent: #ffffff;\n  --tg-border: #e4e4e2;\n  --tg-text: #1c1c1a;\n  --tg-muted: #69675f;\n  --tg-composer-icon: #55534c;\n  --tg-quote-rule: #85837c;\n  --tg-accent: #0b74bd;\n  --tg-link: #0b74bd;\n  --tg-doodle: rgba(0, 0, 0, 0.08);\n  --tg-gesture-pill: #1c1c1a;\n}\n\n/* iPhone shell: titanium band, polished chamfer, ~5px uniform bezel, side buttons\n   standing proud of the frame (never negative z-index — that paints them behind the\n   page background). Height follows width, so the component sizes to its container. */\n.tgchat-phone {\n  position: relative;\n  width: 100%;\n  aspect-ratio: 356 / 730;\n  padding: 5px;\n  background: var(--tg-phone);\n  border-radius: 16cqw;\n  box-shadow: var(--tg-lift), var(--tg-phone-band);\n}\n\n.tgchat-phone::after {\n  position: absolute;\n  z-index: 1;\n  inset: 0;\n  border-radius: inherit;\n  box-shadow: var(--tg-phone-chamfer);\n  content: \"\";\n  pointer-events: none;\n}\n\n.tgchat-btn {\n  position: absolute;\n  z-index: 0;\n  width: 3px;\n  background: var(--tg-phone-button);\n  border-radius: 2px;\n}\n.tgchat-btn.action {\n  top: 31%;\n  left: -3px;\n  height: 4%;\n  border-radius: 2px 0 0 2px;\n}\n.tgchat-btn.vol-up {\n  top: 22%;\n  left: -3px;\n  height: 8%;\n  border-radius: 2px 0 0 2px;\n}\n.tgchat-btn.vol-down {\n  top: 32%;\n  left: -3px;\n  height: 8%;\n  border-radius: 2px 0 0 2px;\n}\n.tgchat-btn.power {\n  top: 27%;\n  right: -3px;\n  height: 12.6%;\n  border-radius: 0 2px 2px 0;\n}\n\n.tgchat-screen {\n  position: relative;\n  display: flex;\n  height: 100%;\n  overflow: hidden;\n  flex-direction: column;\n  background: var(--tg-screen);\n  border-radius: 14.6cqw;\n  color: var(--tg-text);\n  font-size: 3.8cqw;\n  line-height: 1.15;\n  --tg-header-top: 11cqw;\n  --tg-composer-h: 10cqw;\n}\n\n/* The thread scrolls under the floating chrome; scrims keep the clock and composer\n   legible. */\n.tgchat-screen::before,\n.tgchat-screen::after {\n  position: absolute;\n  z-index: 2;\n  right: 0;\n  left: 0;\n  content: \"\";\n  pointer-events: none;\n}\n.tgchat-screen::before {\n  top: 0;\n  height: calc(var(--tg-header-top) + 17cqw);\n  background: linear-gradient(\n    to bottom,\n    var(--tg-screen) 0%,\n    var(--tg-screen) 76%,\n    transparent 100%\n  );\n}\n.tgchat-screen::after {\n  bottom: 0;\n  height: calc(var(--tg-composer-h) + 12cqw);\n  background: linear-gradient(\n    to top,\n    var(--tg-screen) 0%,\n    var(--tg-screen) 58%,\n    transparent 100%\n  );\n}\n\n.tgchat-island {\n  position: absolute;\n  z-index: 5;\n  top: 2.5cqw;\n  left: 50%;\n  width: 26cqw;\n  height: 7.6cqw;\n  background: var(--tg-island);\n  border-radius: 4cqw;\n  box-shadow: 0 0 0 1px var(--tg-island-ring);\n  transform: translateX(-50%);\n}\n\n.tgchat-status {\n  position: absolute;\n  z-index: 4;\n  top: 0;\n  right: 0;\n  left: 0;\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  height: 12.4cqw;\n  padding: 0 9.8cqw 0 11.8cqw;\n  color: var(--tg-status-ink);\n  font-size: 3.65cqw;\n  font-variant-numeric: tabular-nums;\n  font-weight: 600;\n  letter-spacing: 0.01em;\n}\n.tgchat-status svg {\n  display: block;\n  height: 3.1cqw;\n  width: auto;\n}\n.tgchat-status .radios {\n  display: flex;\n  align-items: center;\n  gap: 1.4cqw;\n}\n\n.tgchat-header {\n  position: absolute;\n  z-index: 3;\n  top: var(--tg-header-top);\n  right: 0;\n  left: 0;\n  display: flex;\n  align-items: center;\n  gap: 2cqw;\n  padding: 0.5cqw 2.2cqw 2.2cqw;\n}\n.tgchat-round {\n  display: grid;\n  width: 10cqw;\n  height: 10cqw;\n  flex: none;\n  place-items: center;\n  color: var(--tg-text);\n  background: var(--tg-bot-bubble);\n  border: 1px solid var(--tg-border);\n  border-radius: 50%;\n}\n.tgchat-round svg {\n  display: block;\n  width: 4.5cqw;\n  height: 4.5cqw;\n}\n/* iOS-modern header: the title is a centered pill hugging its content (mini\n   avatar + name), and the chat's big avatar stands alone on the right. */\n.tgchat-card {\n  display: flex;\n  min-inline-size: 48%;\n  max-inline-size: 64%;\n  height: 10cqw;\n  flex: none;\n  margin: 0 auto;\n  align-items: center;\n  justify-content: center;\n  gap: 1.8cqw;\n  padding: 0 4cqw 0 1.2cqw;\n  background: var(--tg-bot-bubble);\n  border: 1px solid var(--tg-border);\n  border-radius: 5cqw;\n}\nvideo.tgchat-avatar,\nvideo.tgchat-mini {\n  display: block;\n}\n.tgchat-avatar {\n  display: grid;\n  width: 10cqw;\n  height: 10cqw;\n  flex: none;\n  place-items: center;\n  overflow: hidden;\n  background: var(--tg-user-bubble);\n  border-radius: 50%;\n  color: #fff;\n  font-size: 5cqw;\n  font-weight: 600;\n  line-height: 1;\n  object-fit: cover;\n}\n.tgchat-names {\n  display: flex;\n  min-width: 0;\n  flex-direction: column;\n  align-items: center;\n  gap: 0.3cqw;\n  text-align: center;\n}\n.tgchat-names strong {\n  font-size: 4.2cqw;\n  font-weight: 600;\n  letter-spacing: -0.01em;\n  line-height: 1.2;\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n}\n.tgchat-names span {\n  color: var(--tg-muted);\n  font-size: 3.4cqw;\n  line-height: 1.2;\n}\n\n/* The typing status, as Telegram draws it: accent text in the header sub-line\n   with three pulsing dots. Transient by construction - it exists only during\n   the reading beat of the timeline. */\n.tgchat-names span.typing {\n  display: inline-flex;\n  align-items: center;\n  gap: 1cqw;\n  color: var(--tg-link);\n}\n.tgchat-tdots {\n  display: inline-flex;\n  gap: 0.7cqw;\n}\n.tgchat-tdots i {\n  width: 1cqw;\n  height: 1cqw;\n  background: currentColor;\n  border-radius: 50%;\n  animation: tgchat-tdot 1.1s ease-in-out infinite;\n}\n.tgchat-tdots i:nth-child(2) {\n  animation-delay: 0.18s;\n}\n.tgchat-tdots i:nth-child(3) {\n  animation-delay: 0.36s;\n}\n@keyframes tgchat-tdot {\n  0%,\n  60%,\n  100% {\n    opacity: 0.75;\n    transform: translateY(0);\n  }\n  30% {\n    opacity: 1;\n    transform: translateY(-0.6cqw);\n  }\n}\n@media (prefers-reduced-motion: reduce) {\n  .tgchat-tdots i {\n    animation: none;\n  }\n}\n\n/* Telegram's doodle wallpaper, the client's own technique: the pattern svg is\n   a MASK over a per-theme tint, tiled. Sits under the bubbles (messages carry\n   z-index 1) and under every scrim. */\n.tgchat-wall {\n  position: absolute;\n  z-index: 0;\n  inset: 0;\n  background-color: var(--tg-doodle);\n  mask-position: center;\n  mask-repeat: repeat;\n  mask-size: 120cqw auto;\n  pointer-events: none;\n}\n\n/* The thread scrolls (scrollbar hidden, like the client); the inner column\n   bottom-aligns a short conversation so the first bubbles sit above the\n   composer, never at the top of an empty screen. */\n.tgchat-messages {\n  position: relative;\n  z-index: 1;\n  min-height: 0;\n  flex: 1;\n  overflow: hidden;\n  scrollbar-width: none;\n}\n.tgchat[data-settled] .tgchat-messages {\n  overflow-y: auto;\n}\n.tgchat-messages::-webkit-scrollbar {\n  display: none;\n}\n.tgchat-thread {\n  display: flex;\n  min-height: 100%;\n  flex-direction: column;\n  justify-content: flex-end;\n  gap: 3.4cqw;\n  padding: calc(var(--tg-header-top) + 12.4cqw) 4cqw\n    calc(var(--tg-composer-h) + 11.8cqw);\n}\n\n.tgchat-bubble {\n  inline-size: fit-content;\n  max-inline-size: 94%;\n  padding: 2.2cqw 3.1cqw;\n  border-radius: 6.2cqw;\n  animation: tgchat-bubble-in 350ms var(--tg-ease) both;\n}\n@keyframes tgchat-bubble-in {\n  from {\n    opacity: 0;\n    transform: translateY(6px);\n  }\n}\n@media (prefers-reduced-motion: reduce) {\n  .tgchat-bubble {\n    animation: none;\n  }\n}\n.tgchat-bubble.bot {\n  align-self: flex-start;\n  background: var(--tg-bot-bubble);\n  border-bottom-left-radius: 1.7cqw;\n}\n/* Reaction pills, as Telegram draws them: rounded chips at the bubble's foot,\n   emoji + count, popping in with a small scale. */\n.tgchat-reacts {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 1.4cqw;\n  margin-top: 1.7cqw;\n}\n.tgchat-react {\n  display: inline-flex;\n  align-items: center;\n  gap: 1.1cqw;\n  padding: 0.7cqw 2.2cqw;\n  border-radius: 4cqw;\n  background: color-mix(in srgb, var(--tg-accent) 24%, transparent);\n  font-size: 3.4cqw;\n  line-height: 1.4;\n  /* Telegram Web's bouncy scale-in: overshoot past 1, settle. */\n  animation: tgchat-pop 320ms cubic-bezier(0.34, 1.56, 0.64, 1) both;\n}\n.tgchat-react .n {\n  color: var(--tg-link);\n  font-weight: 600;\n}\n.tgchat-bubble.user .tgchat-react {\n  background: rgba(255, 255, 255, 0.24);\n}\n.tgchat-bubble.user .tgchat-react .n {\n  color: var(--tg-on-accent);\n}\n@keyframes tgchat-pop {\n  0% {\n    opacity: 0;\n    transform: scale(0.2);\n  }\n  60% {\n    opacity: 1;\n    transform: scale(1.18);\n  }\n  100% {\n    transform: scale(1);\n  }\n}\n@media (prefers-reduced-motion: reduce) {\n  .tgchat-react {\n    animation: none;\n  }\n}\n.tgchat-bubble.user {\n  align-self: flex-end;\n  color: var(--tg-on-accent);\n  overflow-wrap: anywhere;\n  background: var(--tg-user-bubble);\n  border-bottom-right-radius: 1.7cqw;\n}\n.tgchat-bubble.link {\n  max-inline-size: min(94%, 24ch);\n  overflow-wrap: anywhere;\n}\n.tgchat-bubble.link:has(.tgchat-preview) {\n  max-inline-size: 84%;\n}\n\n/* Links, as Telegram paints them: link-blue in received bubbles, white\n   underlined in yours. */\n.tgchat-link {\n  color: var(--tg-link);\n  text-decoration: underline;\n  text-decoration-thickness: 1px;\n  text-underline-offset: 2px;\n}\n.tgchat-bubble.user .tgchat-link {\n  color: var(--tg-on-accent);\n}\n\n/* The webpage preview under a link: a tinted card with a rule on the left,\n   site name in link-blue, bold title, muted text, cover image on top.\n   One color (the link) at alpha tiers, like the client. */\n.tgchat-preview {\n  display: block;\n  margin-top: 1.7cqw;\n  color: inherit;\n  text-decoration: none;\n  transition: background 150ms var(--tg-ease);\n  padding: 1.6cqw 2.2cqw 1.8cqw 2.6cqw;\n  overflow: hidden;\n  border-left: 3px solid var(--tg-link);\n  border-radius: 1.4cqw;\n  background: color-mix(in srgb, var(--tg-link) 10%, transparent);\n  --tg-preview-ink: var(--tg-link);\n  --tg-preview-text: var(--tg-text);\n  --tg-preview-muted: var(--tg-muted);\n}\n.tgchat-preview:hover {\n  background: color-mix(in srgb, var(--tg-link) 18%, transparent);\n}\n.tgchat-bubble.user .tgchat-preview:hover {\n  background: rgba(255, 255, 255, 0.22);\n}\n.tgchat-bubble.user .tgchat-preview {\n  border-left-color: var(--tg-on-accent);\n  background: rgba(255, 255, 255, 0.14);\n  --tg-preview-ink: var(--tg-on-accent);\n  --tg-preview-text: var(--tg-on-accent);\n  --tg-preview-muted: rgba(255, 255, 255, 0.78);\n}\n.tgchat-preview img {\n  display: block;\n  width: 100%;\n  aspect-ratio: 16 / 9;\n  margin-bottom: 1.6cqw;\n  border-radius: 1.2cqw;\n  object-fit: cover;\n}\n.tgchat-preview .site {\n  color: var(--tg-preview-ink);\n  font-size: 3.4cqw;\n  font-weight: 600;\n}\n.tgchat-preview .title {\n  margin-top: 0.4cqw;\n  color: var(--tg-preview-text);\n  font-weight: 600;\n  line-height: 1.2;\n}\n.tgchat-preview .desc {\n  display: -webkit-box;\n  margin-top: 0.6cqw;\n  overflow: hidden;\n  -webkit-box-orient: vertical;\n  -webkit-line-clamp: 3;\n  color: var(--tg-preview-muted);\n  font-size: 3.4cqw;\n  line-height: 1.25;\n}\n\n/* The quoted message inside a reply bubble, as Telegram draws it: a rule, the quoted\n   sender, one truncated line. */\n.tgchat-reply {\n  display: flex;\n  flex-direction: column;\n  margin-bottom: 1.4cqw;\n  padding: 0.8cqw 0 0.8cqw 2.2cqw;\n  border-left: 3px solid currentColor;\n  border-radius: 2px;\n  opacity: 0.85;\n}\n.tgchat-reply strong {\n  font-size: 3.4cqw;\n  font-weight: 600;\n}\n.tgchat-reply span {\n  overflow: hidden;\n  max-inline-size: 46cqw;\n  font-size: 3.4cqw;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n/* The inline-bot attribution on the summary. */\n.tgchat-via {\n  margin-bottom: 1.4cqw;\n  color: var(--tg-accent);\n  font-size: 3.4cqw;\n  font-weight: 600;\n}\n\n/* Group chats: a mini avatar rides beside every left bubble, bottom-aligned. */\n.tgchat-rowline {\n  display: flex;\n  max-inline-size: 94%;\n  align-items: flex-end;\n  gap: 1.7cqw;\n}\n.tgchat-rowline .tgchat-bubble {\n  max-inline-size: 100%;\n}\n.tgchat-mini {\n  display: grid;\n  width: 7.3cqw;\n  height: 7.3cqw;\n  flex: none;\n  place-items: center;\n  overflow: hidden;\n  border-radius: 50%;\n  color: #fff;\n  font-size: 3.9cqw;\n  font-weight: 600;\n  line-height: 1;\n  object-fit: cover;\n}\n\n/* Group sender label on a left bubble (color set inline per sender). */\n.tgchat-from {\n  margin-bottom: 0.8cqw;\n  font-size: 3.4cqw;\n  font-weight: 600;\n}\n\n/* A cited timestamp at the end of a summary line: plain link-blue text, exactly\n   as the real bot output renders in Telegram (no pill). */\n.tgchat-cite {\n  margin-left: 1.3cqw;\n  color: var(--tg-link);\n  font-variant-numeric: tabular-nums;\n  text-decoration: none;\n}\n.tgchat-cite:hover {\n  text-decoration: underline;\n  text-underline-offset: 2px;\n}\n\n.tgchat-summary .h {\n  margin: 2.8cqw 0 1.1cqw;\n  font-weight: 700;\n}\n.tgchat-summary .h:first-child {\n  margin-top: 0;\n}\n.tgchat-summary .h .e {\n  margin-right: 1.2cqw;\n}\n.tgchat-summary .i {\n  margin-bottom: 1.7cqw;\n}\n.tgchat-summary .q {\n  margin-top: 2.8cqw;\n  padding: 0.6cqw 0 0.6cqw 3.4cqw;\n  border-left: 3px solid var(--tg-quote-rule);\n}\n.tgchat-summary .q .by {\n  display: block;\n  margin-top: 0.8cqw;\n  color: var(--tg-muted);\n  font-size: 3.4cqw;\n}\n.tgchat-caret {\n  display: inline-block;\n  width: 0.55em;\n  height: 1em;\n  margin-left: 0.1em;\n  background: currentColor;\n  opacity: 0.7;\n  vertical-align: -0.15em;\n}\n/* One line, always: the source may ellipsize but the row NEVER wraps or\n   reflows - a settled summary must not shift. */\n.tgchat-meta {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  flex-wrap: nowrap;\n  gap: 1.7cqw;\n  margin-top: 2.8cqw;\n}\n.tgchat-meta .src {\n  display: inline-flex;\n  min-width: 0;\n  color: var(--tg-link);\n  align-items: center;\n  gap: 1.1cqw;\n  overflow: hidden;\n  text-decoration: none;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.tgchat-meta .src:hover {\n  text-decoration: underline;\n  text-decoration-thickness: 1px;\n  text-underline-offset: 2px;\n}\n.tgchat-meta .time {\n  display: inline-flex;\n  flex: none;\n  align-items: center;\n  gap: 1.1cqw;\n  white-space: nowrap;\n}\n\n/* The reply bar Telegram shows above the composer while you answer a message:\n   arrow, \"Reply to <who>\", one truncated line of what is quoted. */\n.tgchat-replybar {\n  position: absolute;\n  z-index: 3;\n  right: 3.4cqw;\n  bottom: calc(var(--tg-composer-h) + 5.6cqw + 4.8cqw);\n  left: 3.4cqw;\n  display: flex;\n  align-items: center;\n  gap: 2.4cqw;\n  padding: 1.6cqw 3cqw;\n  background: var(--tg-bot-bubble);\n  border: 1px solid var(--tg-border);\n  border-radius: 3cqw;\n  color: var(--tg-link);\n  animation: tgchat-bubble-in 250ms var(--tg-ease) both;\n}\n.tgchat-replybar svg {\n  display: block;\n  width: 4.6cqw;\n  height: 4.6cqw;\n  flex: none;\n}\n.tgchat-replybar div {\n  display: flex;\n  min-width: 0;\n  flex-direction: column;\n  gap: 0.3cqw;\n  padding-left: 2.2cqw;\n  border-left: 2px solid var(--tg-link);\n}\n.tgchat-replybar strong {\n  font-size: 3.3cqw;\n  font-weight: 600;\n}\n.tgchat-replybar span {\n  overflow: hidden;\n  color: var(--tg-text);\n  font-size: 3.3cqw;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n/* Decorative composer: pure chrome, zero focusables — the mockup is a picture.\n   The real bar's furniture stays: paperclip left, emoji inside the field, mic\n   right. */\n.tgchat-composer {\n  position: absolute;\n  z-index: 3;\n  right: 0;\n  bottom: 5.6cqw;\n  left: 0;\n  display: flex;\n  align-items: center;\n  gap: 2.4cqw;\n  padding: 2.4cqw 3.4cqw;\n}\n.tgchat-composer .cbtn {\n  display: grid;\n  width: 10cqw;\n  height: 10cqw;\n  flex: none;\n  place-items: center;\n  color: var(--tg-composer-icon);\n  background: var(--tg-bot-bubble);\n  border: 1px solid var(--tg-border);\n  border-radius: 50%;\n}\n.tgchat-composer .cbtn svg {\n  display: block;\n  width: 5.2cqw;\n  height: 5.2cqw;\n}\n.tgchat-composer .box {\n  display: flex;\n  height: 10cqw;\n  flex: 1;\n  min-width: 0;\n  align-items: center;\n  gap: 1.7cqw;\n  padding: 0 3cqw 0 4.2cqw;\n  color: var(--tg-muted);\n  background: var(--tg-bot-bubble);\n  border: 1px solid var(--tg-border);\n  border-radius: 5cqw;\n  font-size: 3.65cqw;\n}\n.tgchat-composer .box svg {\n  display: block;\n  width: 5.3cqw;\n  height: 5.3cqw;\n  flex: none;\n  color: var(--tg-composer-icon);\n}\n.tgchat-composer .pill {\n  display: inline-flex;\n  height: 10cqw;\n  flex: none;\n  align-items: center;\n  gap: 1.6cqw;\n  padding: 0 3.2cqw;\n  color: var(--tg-on-accent);\n  background: var(--tg-accent);\n  border-radius: 5cqw;\n  font-size: 3.4cqw;\n  font-weight: 600;\n}\n.tgchat-composer .pill svg {\n  display: block;\n  width: 4.4cqw;\n  height: 4.4cqw;\n}\n.tgchat-composer .hint,\n.tgchat-composer .typed {\n  flex: 1;\n  overflow: hidden;\n  white-space: nowrap;\n}\n.tgchat-composer .typed {\n  color: var(--tg-text);\n  text-overflow: clip;\n  direction: rtl;\n  text-align: left;\n}\n.tgchat-composer .typed .tgchat-caret {\n  opacity: 0.9;\n}\n\n/* The spotlight token in a sent mention (the language word): a quiet pill\n   that pulses while the story dwells on it, then rests. */\n.tgchat-emph {\n  padding: 0 0.9cqw;\n  border-radius: 1cqw;\n  background: rgba(255, 255, 255, 0.18);\n  color: inherit;\n  transition: background 300ms var(--tg-ease);\n}\n.tgchat-emph[data-on] {\n  animation: tgchat-emph 1.1s ease-in-out 3;\n}\n@keyframes tgchat-emph {\n  0%,\n  100% {\n    background: rgba(255, 255, 255, 0.18);\n    box-shadow: 0 0 0 0 rgba(255, 255, 255, 0);\n  }\n  50% {\n    background: rgba(255, 255, 255, 0.42);\n    box-shadow: 0 0 0 0.8cqw rgba(255, 255, 255, 0.18);\n  }\n}\n@media (prefers-reduced-motion: reduce) {\n  .tgchat-emph[data-on] {\n    animation: none;\n    background: rgba(255, 255, 255, 0.42);\n  }\n}\n\n.tgchat-gesture {\n  position: absolute;\n  z-index: 4;\n  right: 0;\n  bottom: 0;\n  left: 0;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  height: 5.6cqw;\n}\n.tgchat-gesture span {\n  width: 30cqw;\n  height: 4px;\n  background: var(--tg-gesture-pill);\n  border-radius: 2px;\n}\n",
      "type": "registry:ui"
    }
  ],
  "type": "registry:ui"
}